#include "main.h"

/**
 * _isupper - A function
 * @c: parameter to be evaluated
 * Return: 1 if true or 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
